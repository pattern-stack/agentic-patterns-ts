node_modules/
dist/
*.tsbuildinfo
.turbo/
.claude/worktrees/
.env
.env.local
packages/agent-cli/assets/dashboard/

# Scratchpad produced by `ap init --with-plugin demo` for manual verification.
# Regenerable from the published CLI; not committed.
demo/

# Local session + QA artifacts (not source)
**/.claude/.session/
screenshots/

# Throwaway git worktrees (session harnesses)
.worktrees/

# Local QA artifacts (seeded family dbs)
.qa/

# Source-run dashboard assets (regenerated: build:dashboard + cp, see handoff)
packages/agent-cli/src/assets/
