/**
 * @deprecated Relocated to `../charts/FrontierScatter` (slice 4 — the chart
 * atoms live under `pages/eval/charts/`). This shim keeps existing
 * `panels/FrontierScatter` imports compiling unchanged; import from `charts/`
 * in new code.
 */
export { FrontierScatter } from "../charts/FrontierScatter";
