# Spec — #342 go: revival hygiene — CI `check` workflow, license name fix + BSL posture note, base pin

```yaml
issue: 342                # tracker: pattern-stack/agentic-patterns-ts
epic: 339
stack: harness-cockpit
plan_key: harness-cockpit/hygiene
round: R1 (wave1)
size: S
target_repo: github.com/pattern-stack/chat-patterns   # code lands HERE, not in this repo
target_branch: cockpit/hygiene
base_commit: 187411bf18af0fb4d109e9b556935b6947d7eb68  # pinned revival base = origin/main tip
gate_mode: strict (gate:human)
related:
  - .ai-docs/stacks/harness-cockpit/plan.md §5 Licensing, §6 Versioning (branch docs/harness-cockpit-plan)
  - .ai-docs/stacks/harness-cockpit/scout/constraints-alignment.md §2.2, §3.1–3.2
  - .ai-docs/stacks/harness-cockpit/scout/go-wire-abort.md §5 (reference workflow YAML)
  - .ai-docs/plans/harness-cockpit.yaml (plan-key harness-cockpit/hygiene; YAML key "340", synced as #342)
```

All chat-patterns facts below were **re-verified remotely on 2026-07-22** via the GitHub
contents API at tip `187411b` (still `origin/main`; only branch is `main`; zero PRs; license
reported `NOASSERTION` = custom BSL).

## Goal

Make chat-patterns safe to build on before any behavioral change lands: a CI gate named
`check` (mirroring the TS repo's required-status convention) running `just quality`, the
stale BSL Licensed-Work name fixed with a recorded licensing posture, and the revival base +
PR-to-main rule written down where humans (README) and agents (CLAUDE.md) will see them.
This is the **first PR in the repo's history** — PR-to-main discipline starts with the very
PR that delivers it.

## Verified current state

| Fact | Evidence (remote, at `187411b`) |
|---|---|
| No `.github/` directory at all → no CI | root contents listing: `.claude`, `.gitignore`, `CLAUDE.md`, `Justfile`, … — no `.github` |
| 12 commits, all direct to main; zero PRs | commit history; PR list empty; scout §3.2 |
| `just quality` = `go build ./... && go vet ./... && go test ./...` | `Justfile:43-45` |
| Quality gate passes at base | scout §3.2 (run 2026-07-19: build PASS, 7 test pkgs `ok`) |
| `go 1.25.0`, no `toolchain` directive | `go.mod:3` |
| LICENSE = BSL 1.1; **Licensed Work: `agentic-tui`** (stale — predates rename); Licensor Doug; Change Date 2030-04-10 → Apache 2.0; Additional Use Grant permits production use unless the Work is offered to third parties as a commercial product / hosted service | `LICENSE` lines 1–17 |
| README `## License` (last section, ~line 154) says "free for non-commercial use" — **misstates the grant** (production use is permitted; reselling the Work is what's excluded) | `README.md:154-156` |
| README sections: Quick Start / Transport Modes / Features / Demos / Protocol / Examples / Architecture / License — no Development/Contributing section | README headings |
| CLAUDE.md has "Build & Development Commands" + Architecture; no landing-changes/process section | `CLAUDE.md` head |

## Approach

One small docs+CI PR on branch `cockpit/hygiene`, opened from a **fresh clone** (the old
sandbox clone drifted mid-scout and is treated as disposable). Four files touched, zero Go
code. CI validates itself: the `pull_request` trigger runs the workflow from the PR's own
branch, so acceptance ("`check` green on the PR") is satisfied by the PR that introduces it.

## File-level plan (all paths in chat-patterns)

| File | Action | Change |
|---|---|---|
| `.github/workflows/check.yml` | **create** | Workflow + job both named `check`; see shape below |
| `LICENSE` | modify | Single-token fix: `Licensed Work:        agentic-tui` → `chat-patterns`. Every other byte unchanged (Licensor, (c) 2026 Doug, grant text, Change Date, Change License) |
| `README.md` | modify | (a) new `## Development` section before `## License` — revival base, PR-to-main rule, CI gate; (b) `## License` section rewritten as the one-paragraph posture note (also fixes the "non-commercial" mischaracterization) |
| `CLAUDE.md` | modify | Short "Landing changes" note after the build-commands block — every commit lands via PR to `main`, required check is `check` (= `just quality`), base pinned `187411b` |

Explicitly untouched: `go.mod` (stays `go 1.25.0` — locked), `go.sum`, all Go sources, the
`.claude/` directory in chat-patterns.

## Content requirements

### 1. `.github/workflows/check.yml`

Adopt the reference implementation from `scout/go-wire-abort.md §5` as-is:

- Workflow `name: check`; triggers: `push` on `main` + `pull_request` (all branches).
- Single job with **job id `check`** on `ubuntu-latest` — the job id is the status string
  branch protection will key on later; it must be exactly `check`, matching the TS repo.
- Steps: `actions/checkout@v4` → `actions/setup-go@v5` with `go-version-file: go.mod` and
  `cache: true` → `extractions/setup-just@v3` → `run: just quality`.
- Routing through `just quality` keeps the local and CI gates definitionally identical.
  Documented fallback if the third-party just action is rejected at review: inline
  `go build ./... && go vet ./... && go test ./...` (loses the identity; not preferred).
- **Disposition on Go versions:** single version from `go-version-file` (resolves 1.25.0).
  The earlier §2.2 idea of a 1.25/1.26 matrix is superseded by the locked issue body
  ("go-version-file go.mod"); the 1.26.x line is covered by local dev (verified green,
  scout §3.2) and needs no CI lane.

### 2. `LICENSE`

Line 6 only: `agentic-tui` → `chat-patterns`. This is a parameter correction, not a license
change — BSL 1.1 terms, Licensor, dates, and grant are byte-identical before/after.

### 3. README `## License` — the posture note (OD-3, locked)

Replace the current two-line blurb with one paragraph carrying exactly these elements:

> chat-patterns is licensed under the Business Source License 1.1 (Licensor: Doug;
> Change Date 2030-04-10, converting to Apache 2.0 — see [LICENSE](LICENSE)). The
> Additional Use Grant permits production use — including commercial and internal use,
> such as running this TUI as the harness cockpit for agentic-patterns — provided the
> Licensed Work itself is not offered to third parties as a commercial product or hosted/
> managed service. Posture (recorded 2026-07): BSL 1.1 is retained; relicensing is
> deferred until code would actually cross into an MIT-licensed repo, at which point it
> is a single-signature decision by the sole author.

Implementer may adjust phrasing, not substance. This corrects the existing
"free for non-commercial use" line, which understates the grant.

### 4. README `## Development` + CLAUDE.md landing note

Both surfaces state, briefly:
- Development resumed 2026-07 from pinned base `187411b` (repo was dormant since 2026-04).
- **Every change lands via PR to `main`** from this PR onward; required gate is the
  `check` workflow = `just quality`. No direct pushes to main.
- README version is human-facing (2–4 lines + the existing Justfile pointers); CLAUDE.md
  version is agent-facing (mirrors the TS repo's "Landing changes" section style) — without
  the CLAUDE.md note, coding agents will keep committing straight to main.

## Process / landing (implementer runbook shape)

1. Fresh clone of `github.com/pattern-stack/chat-patterns` into a new directory (do NOT
   reuse any existing sandbox clone).
2. **Base-pin guard:** verify `git rev-parse origin/main` == `187411bf18af0…7eb68`. If the
   tip has moved, HALT and escalate — the pin is a locked plan invariant (plan.md §1 risk 4).
3. Branch `cockpit/hygiene` from that commit. Make the four file changes.
4. Local gate: `just quality` green (local go 1.26.x is fine; go.mod untouched).
5. PR to `main` titled `chore: revival hygiene — CI check workflow, license name fix, base pin`,
   body cross-referencing the tracker issue as `pattern-stack/agentic-patterns-ts#342`
   (full-form ref — it lives in a different repo).
6. Confirm the `check` job runs **on the PR itself** and is green (acceptance line 1).
7. Merge via the PR. No tags, no version bumps (Go consumers pin by commit; none exist yet).

## Acceptance mapping (from the issue, verbatim → mechanism)

| Acceptance line | Satisfied by |
|---|---|
| CI runs and passes on this PR itself (`check` green on the PR) | `pull_request` trigger runs the new workflow from the PR branch; job id `check` |
| LICENSE names the correct Licensed Work; posture note committed | LICENSE line-6 fix + README `## License` paragraph (README section chosen over a separate LICENSE-NOTE file — see open question 2) |
| README states the revival base (`187411b`) and PR-to-main rule | new `## Development` section |

## Tests

No Go code changes → no new Go tests. The deliverable **is** the test: `just quality`
executing green in CI on the PR. Manual verification per the runbook above.

## Out of scope

- charm dependency bumps (`deps` issue, next in the Go chain — depends on this one).
- All wire fixes / parser / abort work (later R1 issues).
- Any relicensing (locked deferral, OD-3) and any code crossing into an MIT repo.
- Enabling branch protection via API (admin follow-up — open question 1).
- The `.claude/` directory and agenti-kit consumer in chat-patterns.

## Locked decisions honored (not relitigated)

- First Go PR lands CI so every subsequent fix merges through a gate; PR-to-main starts here.
- OD-3: keep BSL 1.1; fix Licensed-Work name; one-paragraph posture note; cockpit use is
  inside the Additional Use Grant.
- Base pinned `187411b`; fresh clone.
- `go 1.25.0` stays in go.mod; local 1.26.x verified green.
- Branch name `cockpit/hygiene` (per plan YAML / issue header).

## Open questions (for the Gate-1 human)

1. **Branch protection now or later?** Enabling required-status `check` on chat-patterns
   `main` is one admin API call, sensible immediately after this PR merges. It needs
   Doug's admin token either way. Recommend: enable right after merge; say the word and it
   goes in the implementer's runbook as a post-merge step, else it stays convention-only.
2. **Posture-note home:** this spec puts it in README `## License` (issue allows
   "LICENSE-NOTE or README section"). Confirm, or prefer a separate `LICENSE-NOTE.md`?
3. **README grant wording:** the fix replaces "free for non-commercial use" with
   grant-accurate wording (production use allowed; reselling the Work excluded). This
   *broadens* the stated permission to match the actual license text — confirm intended.
4. **`extractions/setup-just@v3`** (third-party action) acceptable in CI? Fallback is
   inlining the three go commands, at the cost of CI ≠ `just quality` identity.
