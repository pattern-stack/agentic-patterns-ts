---
issue: "385"
title: "Drop CJS output; require Node >= 22 (pre-req for ai@7)"
stack: ai-sdk-v7
key_original: esm-node22
status: strategy-approved (gate:auto)
related:
  - .ai-docs/stacks/ai-sdk-v7/understanding.md (decisions D2, D3)
  - .ai-docs/stacks/ai-sdk-v7/plan.yaml (issue 385; blocks 386)
date: 2026-07-26
---

# Spec — Drop CJS output; require Node >= 22

## Goal

Ship every publishable package (`core`, `runtime`, `server`, `cli`) as ESM-only with
`engines.node: ">=22"`, so the subsequent `ai@7` bump (#386) — which is ESM-only,
Node >= 22 — is unblocked. This lands as a standalone PR, independently revertable
and publishable, per decisions D2/D3 in `understanding.md`.

No runtime behavior changes. This is a packaging + build-config + docs + smoke-gate
change only.

## Approach

Three concentric edits, all mechanical, all verified against current files:

1. **Build**: tsup `format` → `["esm"]` in the three dual-format packages
   (cli is already ESM-only at `packages/agent-cli/tsup.config.ts:6`).
2. **Manifest**: for core/runtime/server, delete `main` + `module`, flatten the
   `exports["."]` conditional to `{types, default}` (no `require` branch); bump
   `engines.node` to `">=22"` in all four package.json files plus the private
   workspace root.
3. **Contract**: the tarball smoke gate (`test/post-publish/run-tarball-smoke.ts`)
   currently *asserts* the CJS contract — it must flip from "verify CJS works" to
   "verify CJS artifacts are absent", directly encoding the acceptance criterion.

CI needs **no change**: both jobs already run `node-version: 24`
(`.github/workflows/ci.yml:27` and `:83`) — verify-only, satisfied.

## File-level plan

### Modify — build configs

| File | Change |
|---|---|
| `packages/agent-core/tsup.config.ts:5` | `format: ["esm", "cjs"]` → `format: ["esm"]` |
| `packages/agent-server/tsup.config.ts:5` | same |
| `packages/agent-runtime/tsup.config.ts:5` | same; **also delete `shims: true` and its comment block (lines 13–20)** — the comment's entire rationale is the CJS build's missing `import.meta` shim ("esbuild does NOT shim `import.meta` for cjs"); with ESM-only output, `import.meta.url` is native and the shim is dead weight. Keep `external: ["bun:sqlite"]` (lines 21–24) — unrelated to format. |
| `packages/agent-cli/tsup.config.ts:6` | `target: "node20"` → `target: "node22"` (aligns the only explicit build target with the new floor) |

### Modify — package manifests

For each of `packages/agent-core/package.json`, `packages/agent-runtime/package.json`,
`packages/agent-server/package.json` (identical shape, lines 21–35 in each):

- Delete `"main": "./dist/index.cjs"` (line 21).
- Delete `"module": "./dist/index.js"` (line 22) — a dual-build-era bundler hint,
  redundant once `exports` is the single source of truth.
- Keep top-level `"types": "./dist/index.d.ts"` (harmless; aids older editors/tooling).
- Replace the `exports` block with the flat single-condition form:

```json
"exports": {
  ".": {
    "types": "./dist/index.d.ts",
    "default": "./dist/index.js"
  }
}
```

- `"engines": { "node": ">=20" }` → `">=22"` (line 13 in each).

For `packages/agent-cli/package.json`: engines bump only (line 13; no
`main`/`module`/`exports` exist — bin-only package).

For the root `package.json:31`: `"node": ">=20"` → `">=22"` (private workspace
root, but keep the declared floor consistent). Leave `"bun": ">=1.1.0"` as is.

### Modify — tarball smoke gate

`test/post-publish/run-tarball-smoke.ts`:

- Line 38 comment "Importable dual-format libraries." → "Importable ESM libraries."
- Header doc (lines 10–13): rewrite to describe the ESM-only + no-CJS-artifact contract.
- **Section 3** (lines 118–152): keep the ESM import check; delete the CJS block —
  the `cjsChecks` generation, `cjs-check.cjs` write, and its `execFileSync` run
  (lines 131–152). Update the `ok(...)` message.
- **Section 4** (lines 156–161): type contract becomes `["index.d.ts"]` only, and
  **add a negative assertion**: for each LIB_PKG, fail if any `*.cjs`, `*.cjs.map`,
  or `*.d.cts` file exists under the installed `dist/` — this is the acceptance
  criterion "published artifact contains no `.cjs`/`.d.cts`" enforced at the gate
  that runs before every npm upload (`scripts/publish.sh:181`).

### Modify — stale docs

| File:line | Current | New |
|---|---|---|
| `CLAUDE.md:46` | "tsup produces both ESM and CJS outputs" | "tsup produces ESM output (ESM-only as of v0.30)" |
| `CLAUDE.md:80` | "tsup — bundler (ESM + CJS dual output)" | "tsup — bundler (ESM-only)" |
| `README.md:411` | "tsup produces both ESM and CJS outputs" | same fix as CLAUDE.md:46 |
| `README.md:450` | "verifies the ESM/CJS import, type, and CLI-bin contract" | "verifies the ESM import, type, no-CJS-artifact, and CLI-bin contract" |
| `README.md:471` | "tsup — bundler (ESM + CJS)" | "tsup — bundler (ESM-only)" |
| `.claude/sdlc.yml:6` | comment "ESM + CJS dual output" | "ESM-only output" |

If README states a Node version prerequisite anywhere near install instructions,
update to Node >= 22 (grep for "Node" in README during implementation; the
targeted sweep above found no explicit ">= 20" prose outside `engines`).

### Version bumps (staged, per repo convention)

`just bump-both` (justfile:97, defaults `minor minor`) — runs
`scripts/bump.sh --lockstep minor --core minor`. Rationale: engines floor raise +
dropped require entry point is breaking for CJS consumers; under 0.x convention a
minor bump signals that. Result: runtime/server/cli 0.29.3 → 0.30.0, core
0.13.1 → 0.14.0. Publish fires automatically on merge (idempotent, gated on the
updated smoke).

## Explicitly out of scope — do not touch

- `packages/agent-runtime/src/providers/cc-shim.ts:176` — `shim.cjs` is a
  **runtime-generated** script written to a tmpdir and executed as its own Node
  child process (`args: [shimScript]`); the `.cjs` extension deliberately forces
  CJS parsing for that standalone script. Unrelated to package dist output. Keep.
- `packages/agent-cli/src/commands/init.ts` scaffold — verified clean: the
  rendered consumer `package.json` (`renderPackageJson`, lines 279–306) has no
  `engines`, no `.cjs` references, and already emits `"type": "module"`. The stale
  `ai: "^4.0.0"` pin there belongs to #386, not this PR.
- Historical prose: `docs/adr/0005-session-scope.md` "ESM/CJS dual-build identity
  hazard" mentions are an immutable decision record; "dual-package boundary" in
  `docs/playground-redesign.md` / `agent-cli/src/helpers/provenance.ts` means
  src-vs-dist duplication, not ESM/CJS. Leave all of these.
- `.github/workflows/ci.yml` — already on Node 24 (lines 27, 83). No edit.
- No source-code changes in any `src/` (except none needed) — dependency versions,
  SDK imports, and codemods are all #386.

## Interfaces

No new interfaces. The consumer-visible contract change is subtractive:

```json
// core / runtime / server package.json — after
{
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "engines": { "node": ">=22" }
}
```

`require("@agentic-patterns/*")` from CJS consumers on Node < 22 stops resolving —
intended and release-noted via the minor bump. (Node >= 22 CJS consumers can still
`require()` the ESM entry via `require(esm)`, which is why the smoke gate now
checks artifact absence rather than require-failure.)

## Tests

1. `bun run check` — build + typecheck + lint + vitest across the workspace; green.
2. After `bun run build`: `find packages/*/dist -name '*.cjs*' -o -name '*.d.cts'`
   returns nothing (the new smoke-gate assertion automates this at publish time).
3. `bun test/post-publish/run-tarball-smoke.ts` (after build) — exercises the
   updated gate end-to-end: pack → install into throwaway consumer → ESM import ✓,
   `.d.ts` ✓, no `.cjs`/`.d.cts` in installed dist ✓, cli bin ✓.
4. Optional belt-and-braces: `bash scripts/publish.sh ci --dry-run` (full CI
   publish path incl. smoke, no upload).

No new vitest suites — the smoke script is the packaging contract's test surface.

## Open questions

None. All plan-level ambiguities resolved against the working tree:
CI already >= 22; init.ts scaffold already clean; the one `.cjs` string in `src/`
is intentional runtime behavior; `shims: true` removal is safe (its sole
documented purpose was the CJS build; no bundled `__dirname` usage in runtime src).
