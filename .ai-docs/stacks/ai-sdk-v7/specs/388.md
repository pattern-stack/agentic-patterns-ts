---
issue: "388"
title: "Surface detailed token accounting (cache + reasoning) through events → exporters → dashboard"
stack: ai-sdk-v7
key_original: token-detail
status: strategy-approved (gate:auto)
related:
  - .ai-docs/stacks/ai-sdk-v7/understanding.md (usage-fields facts §"Usage surface", §"Cumulative breaking changes")
  - .ai-docs/stacks/ai-sdk-v7/plan.yaml (issue 388; depends on 386, parallel with 387/389/390)
  - .ai-docs/stacks/ai-sdk-v7/specs/386.md (this PR stacks on branch `dugshub/386-ai-sdk-v7-bump`; 386 explicitly deferred detail fields here)
date: 2026-07-26
---

# Spec — Detailed token accounting (cache + reasoning): events → exporters → dashboard

All line numbers cite branch `dugshub/386-ai-sdk-v7-bump` @ `292274b` (the migrated
v7 runtime). Verified against the installed `ai@7.0.37` / `@ai-sdk/provider@4.0.3` /
`@ai-sdk/anthropic@4.0.21` d.ts + dist on that branch.

## Goal

The runtime reads only flat `inputTokens`/`outputTokens` everywhere (verified:
that is the complete set of usage reads in `agent-runner.ts`; understanding.md
"Token accounting today"). ai@7's app-level `LanguageModelUsage` additionally
carries `inputTokenDetails.{noCacheTokens,cacheReadTokens,cacheWriteTokens}` and
`outputTokenDetails.{textTokens,reasoningTokens}` (byte-verified,
`ai/dist/index.d.ts:319-369`). Surface those five detail counts as **optional**
fields on the usage-carrying `agent.*` events, thread them through the console /
Langfuse / OTel exporters and the SSE wire, and render a minimal breakdown in the
dashboard trace view.

**The load-bearing invariant is absent ≠ zero.** In ai@7 the detail *objects*
are always present but every member is `number | undefined` — a provider that
doesn't report cache/reasoning yields `undefined` members, and our events must
then **omit** the detail payload entirely, never zero-fill. Two verified
consequences drive this:

- `@ai-sdk/anthropic@4` populates all of them (`convertAnthropicUsage`,
  `@ai-sdk/anthropic/dist/index.js:1968-2017`): `cache_read_input_tokens` →
  `cacheRead`, `cache_creation_input_tokens` → `cacheWrite`,
  `output_tokens_details.thinking_tokens` → `reasoning`.
- Our still-V2 `ClaudeCodeLanguageModel` emits only flat
  `usage: {inputTokens, outputTokens}` (`src/providers/claude-code.ts:633-635`),
  and ai@7's V2→V3 shim (`convertV2UsageToV3`, `ai/dist/index.js:762-775`) maps
  only V2's optional `cachedInputTokens`/`reasoningTokens` (which we never set)
  — so the CC provider reports **no** detail until #387 promotes it to V4. #388
  is `parallel_with` #387 and must NOT assume it: CC runs simply carry no
  `usageDetails`, and everything downstream renders exactly as today.

**Stacked on #386**: branch `dugshub/388-token-detail-accounting` from
`dugshub/386-ai-sdk-v7-bump`. Do not merge before #386.

## Current pipeline, traced end to end (what exists today)

1. **Runner reads flat usage** — `packages/agent-runtime/src/runner/agent-runner.ts`:
   - `run()`: `result.usage?.inputTokens/outputTokens` per iteration (:510-511),
     accumulated into `totalInputTokens/totalOutputTokens` (:512-513).
   - `runStructured()`: no-tools path (:1117-1118); capable path
     `result.usage ?? steps.reduce(...)` (:1144-1154); tier-1 via `this.run()`'s
     `RunResult` (:1170-1171); tier-2 (:1230-1231).
   - `stream()`: `finish-step` part → `stepUsage = part.usage` (:1522-1525;
     local typed `{inputTokens?, outputTokens?}` at :1430), accumulated
     (:1630-1634). v7's `TextStreamFinishStepPart.usage` is a full
     `LanguageModelUsage` (`ai/dist/index.d.ts:2897-2905`) — details are already
     arriving there and being dropped.
2. **Events carry flat counts** — `src/events/types.ts`: `LLMCallEndEvent
   .inputTokens/.outputTokens` (:240-241) per LLM call; `MessageCompleteEvent
   .inputTokens/.outputTokens` (:68-69) as run totals. Emission sites:
   `agent.llm.end` at :474 (error path, zeros), :545 (run), :1540 (stream error),
   :1640 (stream); `agent.message.complete` at :578/:747/:801/:871/:899 (run),
   :1259 (runStructured), :1669/:1866/:1914/:1982 (stream).
3. **Exporters map flat** — `src/exporters/`:
   - `console.ts:84` — verbose `Tokens: N in / M out` line on message.complete.
   - `langfuse.ts:216-222` — `_onLlmEnd` → `usage_details: {input, output}`;
     `:327-335` — `_onMessageComplete` → trace metadata `input_tokens`/`output_tokens`.
   - `otel.ts:128-129` — `gen_ai.usage.input_tokens/output_tokens` on llm.end;
     `:184-185` — `agent.input_tokens/output_tokens` on message.complete.
4. **SSE wire** — `src/transport/sse-formatter.ts`: `message.complete` payload
   `input_tokens`/`output_tokens` (:252-253), `llm.end` payload (:385-386).
   The server is a pure pass-through (`packages/agent-server/src/sse.ts:22,37`
   re-uses runtime `toSSEMapping`) — **zero server code changes**.
5. **Dashboard** — `packages/agent-dashboard/src`:
   - Typed client union: `api/sse-events.ts` `message.complete` data (:17-26),
     `llm.end` data (:215-224).
   - Trace fold: `graph/trace-from-events.ts` accessors `ctxTok`/`outTok` read
     both casings (:128-129), set `TraceStep.ctxTokens/outTokens` on `llm.end`
     (:241-242, :252-253). Replay traces rebuild from **persisted full-event
     JSON** (`persistedToEventLike` :59-68 spreads the stored camelCase
     `AgentEvent` verbatim) — so additive event fields reach replay for free,
     no storage/schema change.
   - Render: `components/organisms/TraceWaterfall.tsx` meta row
     `{ctx · out}` (:127-131) and right-column span (:543-553).

## Approach

One new shared type + one small runner helper module; then purely additive
threading. No new event types, no new SSE wire names (the wire manifest guards
*names* only — `sse-wire-manifest.test.ts:25-30` — so no manifest regeneration).

**Convention note:** the events layer is deliberately **plain TS interfaces**,
not Zod (`events/types.ts` top-of-file; Zod lives in `admin/schemas.ts` etc.).
Follow the local convention: interface fields, `readonly`, optional (`?`), with
`Object.freeze` nowhere in this layer — exactly like `costUsd?` (#323) and
`artifacts?` (ADR-0006), the two prior additive event-payload extensions this
change should imitate (including their conditional-spread emission style,
`agent-runner.ts:756`).

### The five-field detail shape (flat, one object)

Flat mirrors the events convention (top-level `inputTokens`/`outputTokens`) and
keeps SSE/dashboard mapping trivial; the five SDK member names are globally
unambiguous so no input/output nesting is needed.

### Presence rule (the absent ≠ zero mechanics)

- `detailsFromUsage(usage)` returns `undefined` when **all five** members are
  `undefined` → the event field is omitted entirely (conditional spread).
- When at least one member is defined, the object carries **only the defined
  members** (no zero-filling of the others).
- Aggregation across iterations/steps mirrors the SDK's own
  `addTokenCounts` (`ai/dist/index.js:2616-2618`): per field, `undefined +
  undefined = undefined`; otherwise sum treating `undefined` as 0. This is the
  exact semantics ai@7 itself uses in `addLanguageModelUsage` — cite it in the
  helper's doc comment so nobody "fixes" it later.

## File-level plan

### `packages/agent-runtime` — events (layer 5)

**Modify `src/events/types.ts`**
- Add `TokenUsageDetails` (see Interfaces) next to the event interfaces.
- `LLMCallEndEvent` (:237-245): add `readonly usageDetails?: TokenUsageDetails;`
  — per-call detail.
- `MessageCompleteEvent` (:65-96): add the same field — run-level totals,
  doc-commented like `costUsd` (:73-78) with the absent-≠-zero rule.

**Modify `src/events/index.ts`** — export `TokenUsageDetails` in the type barrel
(alongside `LLMCallEndEvent`, :26). Check `src/index.ts` re-exports the events
barrel (it does — no further change expected; verify with typecheck).

### `packages/agent-runtime` — runner (layer 7)

**Create `src/runner/usage-details.ts`** (~40 lines + doc comments):
- `detailsFromUsage(usage: LanguageModelUsage | undefined): TokenUsageDetails | undefined`
- `mergeUsageDetails(a: TokenUsageDetails | undefined, b: TokenUsageDetails | undefined): TokenUsageDetails | undefined`
Both pure; unit-testable without a model. (Placed in runner, not events: it
imports the `ai` type, and events (5) must stay SDK-free.)

**Modify `src/runner/types.ts`**
- `RunResult` (:40-59): add `readonly usageDetails?: TokenUsageDetails;` —
  run-total, same object emitted on the final `message.complete`. Import the
  type from `../events/types.js`. This is what lets runStructured's tier-1
  delegate (`this.run()`) hand its detail back up (:1170-1171 today reads
  `tier1.inputTokens`). All other `RunResult` consumers (workflows, eval,
  conversation) are unaffected — optional additive.

**Modify `src/runner/agent-runner.ts`** — thread per path:

| Path | Change |
|---|---|
| `run()` | Beside `totalInputTokens` accumulators, add `let totalUsageDetails: TokenUsageDetails \| undefined`. At :510-511 also `const iterDetails = detailsFromUsage(result.usage)`; `totalUsageDetails = mergeUsageDetails(totalUsageDetails, iterDetails)`. Spread `...(iterDetails ? { usageDetails: iterDetails } : {})` into the `agent.llm.end` at :545-557. Spread the run total into every `agent.message.complete` — :578 (stop), :747 (terminal_tool), :801 (terminal_tool_error), :871 (cancelled), :899 (max_iterations) — and into each adjacent `RunResult` return (:591, :760, :814, :884, :912). The error-path llm.end at :474 stays detail-free (zeros today; leave the field absent — absent ≠ zero holds trivially). |
| `stream()` | Widen the `stepUsage` local (:1430) to `LanguageModelUsage \| undefined` (it already receives exactly that at :1523; the narrow hand-written type is what's discarding detail). Derive `iterDetails = detailsFromUsage(stepUsage)` beside :1631-1632, merge into a stream-scope `totalUsageDetails`, spread into `agent.llm.end` :1640-1651 and the four `message.complete` sites :1669/:1866/:1914/:1982. The stream error llm.end (:1540) stays detail-free. |
| `runStructured()` | no-tools: `totalUsageDetails = detailsFromUsage(result.usage)` beside :1117. capable: take details from `result.usage` when present; in the belt-and-braces reduce fallback (:1146-1152), fold `mergeUsageDetails(acc, detailsFromUsage(s.usage))` alongside the flat sums. tier-1: `mergeUsageDetails(totalUsageDetails, tier1.usageDetails)` beside :1170-1171. tier-2: merge `detailsFromUsage(tier2.usage)` beside :1230-1231. Spread into the `message.complete` at :1259 and the `StructuredRunResult` return at :1272. |

### `packages/agent-runtime` — exporters (layer 10)

**Modify `src/exporters/console.ts`** (`_onMessageComplete`, :73-89): when
`event.usageDetails` present and verbose, extend the token line, e.g.
`Tokens: 12400 in / 320 out (cache 11900 read · 500 write · reasoning 140) | Model: …`
— render only defined members.

**Modify `src/exporters/langfuse.ts`**:
- `_onLlmEnd` (:211-229): when `event.usageDetails` present, extend
  `usage_details` beyond `{input, output}`. **Double-count hazard (verified):**
  `@ai-sdk/anthropic@4` reports `inputTokens` **inclusive** of cache tokens
  (`total: inputTokens + cacheCreationTokens + cacheReadTokens`,
  `@ai-sdk/anthropic/dist/index.js:2004-2009`), while Langfuse's Anthropic-style
  pricing keys expect `input` to be the *non-cached* count. Mapping when details
  are present:
  `input: details.noCacheTokens ?? (event.inputTokens - (cacheRead ?? 0) - (cacheWrite ?? 0))`,
  `cache_read_input_tokens: details.cacheReadTokens`,
  `cache_creation_input_tokens: details.cacheWriteTokens`,
  `output: event.outputTokens`, `reasoning_tokens: details.reasoningTokens` —
  omitting undefined keys. When details are absent the update is byte-identical
  to today (`{input, output}`), so non-reporting providers are untouched. Key
  names are Open question 1 (verify against Langfuse ingestion + managed model
  definitions so cost inference prices cache reads/writes natively).
- `_onMessageComplete` (:327-335): add the defined detail members
  (snake_case) into the existing `updateTrace` metadata block beside
  `input_tokens`/`output_tokens`.
- The structural `LangfuseClient`/`LangfuseSpan` interfaces (:38-56) take
  `Record<string, unknown>` — **no interface change**.

**Modify `src/exporters/otel.ts`** (`_onLlmEnd`, :124-134): after the two
existing `gen_ai.usage.*` attributes, set — only when defined (OTel attributes
cannot be undefined) —
`gen_ai.usage.cache_read_input_tokens`, `gen_ai.usage.cache_creation_input_tokens`,
`gen_ai.usage.reasoning_tokens` (and optionally `.no_cache_input_tokens` /
`.text_output_tokens` for completeness). The Gen AI semconv has **no stable
cache/reasoning token attributes** — same posture as the existing non-standard
`gen_ai.usage.cost` (:186-191): use the OpenLLMetry de-facto names above with a
comment. `_onMessageComplete` (:179-194): mirror under the `agent.*` namespace
used there (`agent.cache_read_tokens` etc.), or reuse the `gen_ai.usage.*`
names — implementer's pick, one line of comment either way.

**No change**: `sse.ts` (delegates to `SSEFormatter`, :35,70), `run-store.ts`
(aggregate metrics stay flat — see Out of scope), `sqlite.ts`, `http.ts`.

### `packages/agent-runtime` — transport

**Modify `src/transport/sse-formatter.ts`**:
- `message.complete` payload (:249-255 block): after the existing keys, add
  `if (event.usageDetails) payload.usage_details = toSnakeDetails(event.usageDetails)`
  — a tiny local mapper emitting only defined members:
  `{no_cache_tokens?, cache_read_tokens?, cache_write_tokens?, text_tokens?, reasoning_tokens?}`.
- `llm.end` payload (:380-390): same conditional key.
- No new wire *names* → `sse-event-manifest.json` and
  `sse-wire-manifest.test.ts` untouched.

### `packages/agent-dashboard`

**Modify `src/api/sse-events.ts`** — extend the `message.complete` (:17-26) and
`llm.end` (:215-224) data types with
`usage_details?: { no_cache_tokens?: number; cache_read_tokens?: number; cache_write_tokens?: number; text_tokens?: number; reasoning_tokens?: number }`
(mirrors the runtime wire per the file's header contract, :1-9).

**Modify `src/graph/types.ts`** — `TraceStep` (:39-61): beside
`ctxTokens?/outTokens?` (:48-49) add
`tokenDetails?: { noCache?: number; cacheRead?: number; cacheWrite?: number; text?: number; reasoning?: number }`.

**Modify `src/graph/trace-from-events.ts`** — add an accessor beside
`ctxTok`/`outTok` (:128-129) reading **both casings** (camelCase `usageDetails`
from persisted events / snake_case `usage_details` from live SSE — same dual-read
discipline as :128-138), returning the `TraceStep.tokenDetails` shape or
`undefined`; populate it at both `llm.end` fold sites (:241-242 in-place
finalize, :252-253 push fallback).

**Modify `src/components/organisms/TraceWaterfall.tsx`** — minimal breakdown,
two touch points, defined-members-only:
- meta row (:127-131): extend the tokens chip, e.g.
  `12,400 ctx (11,900 cached) · 320 out (140 rsn)` — show `cacheRead` as
  "cached" and `reasoning` as "rsn"; omit parentheticals when absent (today's
  string exactly).
- expanded right column (:543-553): same treatment on the second line.
No new components, no layout change — this repo's dashboard convention (compact
mono chips, `T.fz.micro`) applies; don't over-design.

**Out of dashboard scope**: `chat/model.ts` message meta (:156, :708-714),
`MessageRow.tsx:19`, `NodeInspector.tsx`, `TokensPage.tsx` (admin aggregates) —
see Out of scope.

### Tests

| Suite | Addition |
|---|---|
| **new** `src/runner/__tests__/usage-details.test.ts` | Pure-function coverage: all-absent → `undefined`; partial → only-defined members; merge semantics incl. `undefined+undefined=undefined` and `undefined+n=n` (mirrors `addTokenCounts`). |
| `src/runner/__tests__/agent-runner.test.ts` | run() path: a `MockLanguageModelV3` fixture whose V3 usage carries `cacheRead`/`cacheWrite`/`reasoning` → assert `agent.llm.end` and `agent.message.complete` carry `usageDetails` and `RunResult.usageDetails` matches; a fixture with nested totals only (all detail members `undefined`) → assert the events have **no** `usageDetails` key (absent ≠ zero proof). |
| `src/runner/__tests__/agent-runner-stream.test.ts` | Extend the shared `usageV3` helper (:43-53) with an optional details arg; one streaming test with details on the `finish` part → events carry them; one without → absent. **Caution:** the existing helper populates `noCache`/`text` with the flat totals (:47,:51) — those are *defined* members, so after this change many existing streams will legitimately emit `usageDetails: {noCacheTokens, textTokens}`. Any strict-equality event assertions that break are updated to include it (that's correct new behavior, not a regression); assertion *totals* must not change. |
| `src/runner/__tests__/schema-guard.test.ts` / structured paths | One runStructured capable-path case asserting detail merge across steps (reduce fallback included). |
| `src/exporters/__tests__/exporters.test.ts` | Langfuse: `_onLlmEnd` with details → `usage_details` carries the cache/reasoning keys and `input` is the non-cached count; without details → byte-identical `{input, output}`. OTel: attributes set only when defined (assert the attribute map has no `gen_ai.usage.cache_*` key on a detail-free event). Console: verbose line with/without breakdown. |
| `src/transport/__tests__/sse-formatter.test.ts` | `llm.end`/`message.complete` frames carry `usage_details` (snake_case, defined-only) when present; key absent otherwise. |
| dashboard `src/graph/__tests__/trace-from-events.test.ts` (or sibling) | Fold from live snake_case AND persisted camelCase rows → `TraceStep.tokenDetails`; absent → undefined. Follow `model.test.ts`'s dual-shape precedent. |

## Interfaces

The one new exported type (events layer) + the runner helper signatures:

```ts
// src/events/types.ts
/**
 * Optional cache/reasoning token detail (#388). Populated ONLY from what the
 * provider reported — every member is independently optional and the carrying
 * event omits the whole object when the provider reported nothing.
 * ABSENT MEANS UNREPORTED, NEVER ZERO. Input-side members partition
 * `inputTokens` when all three are present (v4-spec providers, e.g.
 * @ai-sdk/anthropic: total = noCache + cacheRead + cacheWrite); output-side
 * members partition `outputTokens` likewise (text + reasoning).
 */
export interface TokenUsageDetails {
  readonly noCacheTokens?: number;
  readonly cacheReadTokens?: number;
  readonly cacheWriteTokens?: number;
  readonly textTokens?: number;
  readonly reasoningTokens?: number;
}

// LLMCallEndEvent + MessageCompleteEvent + RunResult each gain:
readonly usageDetails?: TokenUsageDetails;
```

```ts
// src/runner/usage-details.ts
import type { LanguageModelUsage } from "ai";
import type { TokenUsageDetails } from "../events/types.js";

/** undefined when the provider reported no detail members at all. */
export function detailsFromUsage(usage: LanguageModelUsage | undefined): TokenUsageDetails | undefined;

/** Per-field: undefined+undefined = undefined; else sum treating undefined as 0
 *  (mirrors ai's own addTokenCounts/addLanguageModelUsage). */
export function mergeUsageDetails(
  a: TokenUsageDetails | undefined,
  b: TokenUsageDetails | undefined,
): TokenUsageDetails | undefined;
```

SSE wire (additive key on two existing events, snake_case, defined-only):

```
llm.end / message.complete payloads +=
  usage_details?: { no_cache_tokens?, cache_read_tokens?, cache_write_tokens?,
                    text_tokens?, reasoning_tokens? }
```

## Explicitly out of scope — do not touch

- **`ClaudeCodeLanguageModel` / cc-shim / cc-session** — stay V2-flat; #387
  promotes to V4 with nested usage, at which point detail flows through this
  PR's plumbing with **zero further changes** (the V2→V3→app conversion path is
  already wired; verified via `convertV2UsageToV3` + `asLanguageModelUsage`).
- **CC harness path** (`runner/harness/types.ts:63` `TokenUsage`,
  `cc-harness-translator.ts:226-235`, `harness-event-translator.ts:197-198`,
  `coding-agent-runner.ts`) — the CC SDK does report cache counts, but that
  vocabulary is harness-owned and rides with #387 (whose acceptance includes
  "real cache read/write numbers from Claude usage fields") or a follow-up.
  This PR's events accept detail from ANY emitter, so the seam is ready.
- **Aggregate/admin surfaces** — `run-store.ts` iteration metrics (:68-69),
  `admin/schemas.ts` (:16,:107,:175), `/admin/tokens` + `TokensPage.tsx`,
  `storage/*` — flat totals stay. Replay traces get detail via verbatim event
  JSON, which is what the acceptance criterion (trace view) needs.
- **Chat surfaces** — `chat/model.ts` meta, `MessageRow.tsx` — follow-up nit.
- **Server** — pass-through via runtime `toSSEMapping`; zero edits.
- **Core package** — SDK-free and event-free; zero edits.
- **`totalTokens`** — we don't surface it today; not added (derivable).
- **Langfuse/OTel client interface shapes** — structural `Record<string, unknown>`
  params already admit the new keys.

## Tests / Acceptance

1. **`bun run check`** green (build + dist-contract + typecheck + lint + vitest
   + model-facing-schemas). All existing assertion *totals* unchanged.
2. **Acceptance 1 — absent ≠ zero**: the paired present/absent runner tests
   above; a detail-free provider produces events with no `usageDetails` key
   (assert via `"usageDetails" in event === false`, not `toBeUndefined`-on-zero).
3. **Acceptance 2 — exporter mapping**: exporters.test.ts cases above, incl.
   the Langfuse non-cached-`input` arithmetic and OTel defined-only attributes.
4. **Acceptance 3 — dashboard breakdown**: trace-fold test + a
   TraceWaterfall render check (meta chip shows `(N cached)` / `(N rsn)` when
   present; byte-identical string when absent).
5. **Live smoke vs anthropic** (needs `ANTHROPIC_API_KEY`): run
   `scripts/structured-output-trial.ts` (or a `stream()` probe) twice with a
   prompt-cache-eligible system prompt on the same session — second call's
   `agent.llm.end` shows `cacheReadTokens > 0`; with a thinking-enabled model,
   `reasoningTokens > 0`. This is the "provider reports them" end of Acceptance 1.

## Version staging

- Stacks on #386, which already took lockstep **0.32.0**
  (`packages/agent-runtime/package.json:3` on the branch). This PR: runtime
  changes are additive-feature → `just bump-lockstep minor` → runtime/server/cli
  **0.32.0 → 0.33.0** (server ships no code change but bumps in lockstep per
  repo convention; re-run `npm view` at implementation time — the invariant is
  "one fresh lockstep minor on top of whatever the stack base landed", same
  posture as 385/386, and parallel siblings 387/389/390 each restack the same way).
- **Core untouched** — no core bump.
- **Dashboard** — `private: true` (`packages/agent-dashboard/package.json`),
  never published: no version bump required; no publish step.

## Open questions

None block start; all resolve at implementation time:

1. **Langfuse detail key names** — the exporter's `usage_details` dict accepts
   arbitrary keys; the recommended `cache_read_input_tokens` /
   `cache_creation_input_tokens` (Anthropic-API-native names) should let
   Langfuse's managed Anthropic model definitions price cache traffic natively,
   and `input` must be the non-cached count to avoid double-billing. Verify
   both against current Langfuse usage-details docs + a managed model
   definition before locking the strings (peer is `langfuse@^3.0.0`,
   `package.json:40`; the params style here mirrors the Python SDK — keep it).
2. **OTel attribute names** — confirm whether the semconv version your
   collector targets has since standardized cache/reasoning usage attributes;
   if so prefer the official names over the OpenLLMetry de-facto ones. Either
   way keep the "no stable semconv" comment style of `gen_ai.usage.cost`.
3. **`textTokens` in the UI** — the spec renders cacheRead + reasoning only
   (the two the issue names). If the waterfall chip feels sparse with real
   data, adding `noCache`/`text` to the expanded view is a one-liner — defer to
   implementation taste within the two cited touch points.
4. **Existing stream fixtures emit `noCache`/`text`** — after this change those
   suites' events legitimately gain `usageDetails`; update strict-equality
   assertions rather than nulling the fixture members (the fixtures model a
   V3 provider, and V3 providers do report these).

## Spec Review
<!-- written by: reviewer · gate 1.5 · /sdlc:critique · lens=mixed -->

**Target:** `.ai-docs/stacks/ai-sdk-v7/specs/388.md` @ `dd4a4fe`
**Against:** cited code (`packages/agent-runtime` + `packages/agent-dashboard` src, branch `dugshub/386-ai-sdk-v7-bump`) + freshly-fetched npm tarballs `ai@7.0.37` / `@ai-sdk/anthropic@4.0.21` / `@ai-sdk/provider@4.0.3` (byte-verified against dist, not trusted)
**Verdict:** PASS_WITH_NOTES

A rigorous, unusually high-precision spec. I re-fetched the three packages from
npm and byte-verified every load-bearing claim against the actual dist rather
than trusting the prose. **All eight load-bearing claims hold.** The one thing
worth arguing about is a Langfuse/OTel *asymmetry* the spec introduces (below),
which is a note, not a blocker.

- **THE double-billing claim — VERIFIED, and correctly fixed.**
  `asLanguageModelUsage` (`ai/dist/index.js:2545-2564`) sets the app-level flat
  `inputTokens` to `usage.inputTokens.total` and `inputTokenDetails.noCacheTokens`
  to `usage.inputTokens.noCache`. `convertAnthropicUsage`
  (`@ai-sdk/anthropic/dist/index.js:2003-2009`) builds `total = input_tokens +
  cacheCreationTokens + cacheReadTokens`, `noCache = input_tokens`. So
  `event.inputTokens` **is inclusive** of cache read+write for anthropic@4, and
  the spec's Langfuse mapping `input: noCacheTokens ?? (inputTokens − cacheRead −
  cacheWrite)` is arithmetically exact (both branches equal `noCache`). Sending
  `event.inputTokens` as Langfuse `input` while *also* sending
  `cache_read_input_tokens`/`cache_creation_input_tokens` would double-bill —
  the spec catches this. Cited anthropic dist lines are dead-on.
- **Usage-read inventory — VERIFIED exhaustive.** `grep -nE '\.usage\b'` on
  `agent-runner.ts@292274b` yields exactly `:510-511` (run), `:1117-1118`
  (runStructured no-tools), `:1145`+`:1148-1149` (capable reduce fallback),
  `:1230-1231` (tier-2), `:1523` (stream finish-step). Tier-1 (`:1170`) reads
  `RunResult.inputTokens`, not `.usage` — which is exactly why it is absent from
  the `.usage` grep and why the spec threads it via `RunResult.usageDetails`. No
  read site is missed.
- **Events-are-plain-interfaces — VERIFIED, and no requirement is dodged.**
  `events/types.ts` is `import type` discriminated-union interfaces; `grep zod`
  over `src/events/` is clean. Crucially, I read issue #388's body: its three
  acceptance lines are *absent≠zero events / exporter mapping / dashboard render*
  — there is **no "Zod-schema'd per convention" acceptance line** to dodge. The
  spec's plain-interface decision (matching the `costUsd` #323 and `artifacts`
  ADR-0006 precedents) is correct, not evasive.
- **V2→V3 shim — VERIFIED.** `convertV2UsageToV3` (`ai/dist/index.js:762-776`)
  sets `noCache: void 0`, `cacheWrite: void 0`, `text: void 0` and maps only
  `cachedInputTokens→cacheRead`, `reasoningTokens→reasoning`. Our
  `ClaudeCodeLanguageModel` sets neither, so all five detail members resolve
  `undefined` → `detailsFromUsage` returns `undefined` → no `usageDetails`. CC
  runs render exactly as today. #388 correctly does not assume #387.
- **`number | undefined` + absent≠zero — VERIFIED.** `LanguageModelUsage`
  (`ai/dist/index.d.ts:320-369`): the detail *objects* are always present
  (non-optional) but every member is `number | undefined`. The omit-when-all-
  undefined discipline is internally consistent events → SSE → dashboard.
- **SSE additive-safety — VERIFIED end-to-end.** `sse-formatter.ts`
  `message.complete` uses a mutable `Record<string, unknown>` with conditional-
  spread additive keys (the `cost_usd`/`finish_reason` precedent). The wire
  manifest test (`sse-wire-manifest.test.ts`) asserts event **names** only, so an
  additive payload *key* needs no regen. The dashboard side (`api/sse-events.ts`)
  is a hand-written TS union with **no zod / no `.parse` / no strict schema** —
  unknown keys are structurally ignored, never rejected. Persistence
  (`event-store.ts:511`) is `JSON.parse(...) as Record<string, unknown>`, so
  additive fields reach replay verbatim. Additive really is safe.
- **File-inventory / other consumers — VERIFIED.** Every other reader of
  `RunResult`/event usage (`conversation/*`, `workflows/*`, `storage/eval-*`,
  `storage/run-store.ts`) reads flat `.inputTokens`/`.outputTokens`, which are
  unchanged — the optional additive `usageDetails` is genuinely inert to them.
  `run-store`/`eval`/`admin`/`chat` are deliberately and correctly out of scope.
- **`finish-step` carries full usage — VERIFIED.** `TextStreamFinishStepPart`
  (`ai/dist/index.d.ts:2897-2907`) has `usage: LanguageModelUsage`; detail is
  arriving on `part.usage` and being discarded by the narrow `stepUsage`
  (`agent-runner.ts:1430`). The proposed widen-the-local fix is right.

**Notes (3):**
- [§ File-level plan → exporters → `otel.ts` · spec:339-347] **OTel carries the
  same double-count hazard the Langfuse section fixes, left unaddressed.** The
  spec keeps `gen_ai.usage.input_tokens = event.inputTokens` (verified
  **inclusive** of cache) and *adds* `gen_ai.usage.cache_read_input_tokens` /
  `cache_creation_input_tokens` alongside. Any OTel-side cost view built on the
  OpenLLMetry convention (where `input_tokens` is the *billed, non-cached* count
  and cache tokens price separately) would double-count cache traffic — the exact
  failure the spec eliminates for Langfuse by sending `noCache` as `input`. The
  exporter's own `gen_ai.usage.cost` is safe (it comes from pre-computed
  `event.costUsd`, `otel.ts:186-191`, not from tokens), so this is not a runtime
  bug *today*; but the emitted attribute set is semantically inconsistent between
  the two exporters. _Recommend:_ decide explicitly — either emit a non-cached
  input attribute on OTel too (mirror Langfuse), or keep `input_tokens` inclusive
  and add a one-line comment stating cache attrs are informational and MUST NOT be
  summed onto `input_tokens`. One sentence in the spec removes the ambiguity.
- [§ Open questions:2 · spec:384-390] **OQ2 understates itself.** As written it is
  a *naming* question ("confirm official vs OpenLLMetry attr names"). But the real
  open item on OTel is the *semantic* decision above (inclusive vs non-cached
  input), which naming does not resolve. Elevate OQ2 so the implementer does not
  ship the asymmetry by treating it as pure string-choice. (OQ1's *arithmetic* —
  non-cached `input` + separate cache keys — is settled and verified; only its key
  *strings* genuinely float, which is fine to defer.)
- [§ File-level plan → events · spec re: "Zod per convention"] For the human
  reader/next gate: a reviewer expecting a Zod schema here should know the issue
  body never required one; the events layer is intentionally zod-free and the
  `costUsd`/`artifacts` precedents govern. Recorded so this is not re-litigated at
  Gate 2.5.

**Nits (2):**
- [§ File-level plan → transport · spec:349-355] The `message.complete` SSE block
  is a mutable `Record` (clean `if (…) payload.usage_details = …`), but the
  `llm.end` block (`sse-formatter.ts:381-390`) is an **inline object literal**, not
  a pre-declared mutable record. "Same conditional key" needs a spread
  (`...(event.usageDetails ? { usage_details: … } : {})`) or a tiny restructure
  there — trivial, but not literally "the same" edit.
- [§ Presence rule · spec:~140] The `addTokenCounts` citation `ai/dist/index.js:
  2616-2618` lands at `:2614-2616` in the fetched 7.0.37 tarball (±2 lines; the
  function body and `undefined+undefined=undefined` semantics are exactly as
  described). Harmless drift; the anthropic dist cites are byte-exact.

**Verdict rationale:** every load-bearing claim byte-verified true; the one
money-critical hazard (Langfuse double-billing) is correctly identified and
fixed. Zero blockers; notes-only → **PASS_WITH_NOTES**. Gate 1.5 clears —
implementation-ready, blocked only on #386 landing. Fold the OTel note (N1/N2)
in at implementation so the two exporters agree on cache-inclusive semantics.

**Reviewed by:** reviewer agent · 2026-07-26T21:49:22Z

## Diff Review — Adherence
<!-- written by: reviewer · gate 2.5 · /sdlc:review · lens=adherence -->

**Target:** PR #395 (`gh pr diff 395`, GitHub-computed against `main`) @ `33ae5c1`
**Against:** approved spec `.ai-docs/stacks/ai-sdk-v7/specs/388.md` (Gate 1.5 PASS_WITH_NOTES; notes N1/N2 on OTel consistency are part of the contract)
**Verdict:** PASS_WITH_NOTES

The implementation is a faithful, high-fidelity realization of the spec's
file-level plan. All ten review criteria hold. The deviations that exist are
each either an explicit resolution of a spec Open Question or a documented
improvement that better satisfies the underlying requirement — none is a
violation. Scope discipline is clean; the OTel decision is judged justified.

**Criterion-by-criterion (adherence):**

1. **Scope discipline (no provider-file overlap with #394) — CLEAN.** The
   changed-file set contains no `providers/claude-code.ts`, `cc-shim.ts`,
   `cc-session.ts`, or their tests. #388's plumbing accepts detail from any
   emitter; the CC V2→V4 promotion stays owned by #387/#394.
2. **`TokenUsageDetails` on the three named types — PRESENT.**
   `LLMCallEndEvent` (`events/types.ts:269`), `MessageCompleteEvent` (`:97`),
   `RunResult` (`runner/types.ts:56`) each gain `readonly usageDetails?`;
   `StructuredRunResult = RunResult & {…}` inherits it. Absent≠zero proven both
   directions across `usage-details.test.ts`, `agent-runner*.test.ts`,
   `sse-formatter.test.ts`, `exporters.test.ts`, `trace-from-events.test.ts`
   (assertions use `not.toHaveProperty` / `toBeUndefined`, not zero-checks).
3. **`usage-details.ts` helpers match the design — YES.** `detailsFromUsage`
   returns `undefined` on all-absent, only-defined-members otherwise;
   `mergeUsageDetails` folds via `addField` (`undefined+undefined=undefined`;
   else sum treating `undefined` as 0) — mirrors `addTokenCounts`. The Gate 1.5
   line-drift nit is fixed (cite now `ai/dist/index.js:2614-2616`).
4. **Extraction wired at every usage-read site — EXHAUSTIVE.** `run()` (llm.end
   :567 + all five `message.complete` + `RunResult` returns), `runStructured()`
   (no-tools :1143, capable reduce-fallback :1180, tier-1 merge :1206, tier-2
   :1267, complete :1305, `StructuredRunResult` :1317), `stream()` (`stepUsage`
   widened to `LanguageModelUsage` :1470, llm.end :1698, four `message.complete`
   sites). Error-path llm.end (:474) and stream-error llm.end correctly stay
   detail-free per spec.
5. **SSE additive `usage_details` key — CORRECT, nit fixed.** `toSnakeUsageDetails`
   emits snake_case defined-only; no new wire *names* (manifest untouched). The
   Gate 1.5 nit is honored: `message.complete` keeps its mutable-record
   assignment, while `llm.end` now uses a spread
   (`...(llmEndUsageDetails ? { usage_details: … } : {})`), not a literal edit.
6. **Dashboard chain — COMPLETE.** `sse-events.ts` (`UsageDetailsWire` on both
   `message.complete` + `llm.end`) → `graph/types.ts` (`TraceStep.tokenDetails`)
   → `trace-from-events.ts` (`tokenDetailsOf`, dual-cased camel/snake read at
   both fold sites) → `TraceWaterfall.tsx` (`formatTokenChip`, both touch
   points, byte-identical when absent).
7. **THE OTEL DECISION — JUSTIFIED DEVIATION (recorded, not a violation).** The
   coordinator's Gate 1.5 N1/N2 instruction was to make OTel emit non-cached
   input like Langfuse. The implementer instead ships Langfuse with EXCLUSIVE
   mutually-exclusive buckets (`input = noCache`, separate `cache_*` /
   `output_reasoning_tokens`) and OTel with INCLUSIVE `gen_ai.usage.input_tokens`
   /`output_tokens` plus informational `gen_ai.usage.cache_read.input_tokens` /
   `.cache_creation.input_tokens` / `.reasoning.output_tokens` sub-attributes,
   with a full citation comment in `otel.ts _onLlmEnd`. **Independent judgment:**
   the two platforms genuinely define usage-breakdown semantics oppositely —
   OTel Gen AI semconv treats the parent token totals as inclusive with
   cache/reasoning as sub-counts folded into them; Langfuse's `usage_details`
   are mutually-exclusive priced buckets that must sum to the true total. Making
   OTel exclusive (as literally instructed) would misrepresent the total to OTel
   consumers and contradict the semconv. Each exporter is therefore correct for
   its own consumer, and no surface double-bills. This BETTER satisfies the
   requirement's intent ("each exporter semantically correct") than the literal
   instruction. Accepted as a documented deviation. _Verification caveat below._
8. **Langfuse non-cached input arithmetic — PRESENT & EXACT.**
   `input = details.noCacheTokens ?? inputTokens - (cacheRead ?? 0) - (cacheWrite ?? 0)`
   (correct `??`/`-` precedence). See Note N2 on the (absent) negative guard and
   the symmetric output-side change.
9. **Version-staging conflict flagged — YES.** The PR body explicitly stages
   runtime/server/cli lockstep 0.32.0→0.33.0 AND flags the collision with #394
   (also 0.33.0) for human resolution at merge time — exactly as instructed.
10. **Zero-token / non-reporting path shows NO detail fields — YES.** The
    zero-token `llm.end` SSE test (`inputTokens:0/outputTokens:0`) asserts no
    `usage_details`; the fold leaves `tokenDetails` undefined on a detail-free
    event. No zero-filling anywhere. The LLM-free `scope-echo` example carries
    no `usageDetails` by construction.

**Notes (5):**
- [N1 · `exporters/langfuse.ts:1641-1658` (`buildLangfuseUsageDetails`)] **Output-side
  Langfuse mapping deviates from the spec's literal text — a justified
  improvement.** The spec (§ File-level plan → langfuse) wrote
  `output: event.outputTokens` (inclusive) + `reasoning_tokens`. The impl ships
  `output: textTokens ?? (outputTokens - reasoning)` (EXCLUSIVE) +
  `output_reasoning_tokens`. This applies the same exclusive-bucket discipline
  to output that the spec applied to input, closing a latent reasoning
  double-bill in the spec's own mapping (inclusive `output` alongside a summed
  reasoning bucket would over-count). Documented in the code comment. Recorded
  as an intentional, better-satisfying deviation, not a violation.
- [N2 · `exporters/langfuse.ts:1641-1644`] **No explicit negative-value guard on
  the non-cached input/output subtraction.** Criterion 8 asked for one; it is
  absent (no `Math.max(0, …)`). Real-world risk is low: `@ai-sdk/anthropic@4`
  always populates `noCacheTokens`/`textTokens`, so the `??` short-circuits and
  the subtraction only runs for a hypothetical partial-reporting provider — in
  which case a negative bucket could reach Langfuse. Consider clamping. (Quality-
  lens follow-up; the arithmetic itself faithfully matches the spec.)
- [N3 · `exporters/langfuse.ts` / `otel.ts` — Open Question 1/2] **Langfuse key
  `output_reasoning_tokens` and OTel dotted names are unverifiable end-to-end
  here, and the live smoke was deferred.** OQ1/OQ2 explicitly deferred the exact
  strings to implementation-time doc verification; the cache keys
  (`cache_read_input_tokens`/`cache_creation_input_tokens`) match the spec's
  recommendation, but whether Langfuse's managed model definitions price
  `output_reasoning_tokens` natively is not provable without the live-smoke run
  (no `ANTHROPIC_API_KEY` in this environment — acknowledged in the PR body).
  Track as a post-merge verification.
- [N4 · `bun.lock` + `package.json` bumps] The GitHub-computed diff carries
  unrelated dependency drift (`@anthropic-ai/claude-agent-sdk` 0.3.215→0.3.220,
  `@anthropic-ai/sdk` 0.93.0→0.115.0, rollup/hono minors) — stacked-restack
  noise, not part of #388's semantic change. Harmless; worth a glance at
  merge-time alongside the 0.33.0 lockstep resolution.
- [N5 · verification limits] I could not (a) fetch the live OTel
  `semantic-conventions-genai` registry / Langfuse cost-tracking docs (no web
  tools in this environment) nor (b) re-run the branch suite in place (this
  worktree's `node_modules` is pinned to `main`/`ai@5`, and switching the
  conductor branch is disallowed). My verdict rests on: the diff's test bodies
  (which correctly encode the acceptance criteria), Gate 1.5's byte-verification
  of the inclusive-input premise against fetched tarballs, my knowledge of both
  platforms' contracts (which concurs with the OTel decision), and the PR's
  claim of green `bun run check`. A human/CI with the key + network should
  confirm the live-smoke and the two exporter key strings before publish.

**Nits (1):**
- [`components/organisms/TraceWaterfall.tsx:1071-1077`] The chip renders
  `12,400 (11,900 cached) ctx · 320 (140 rsn) out` — matches the mission's cited
  example; the spec's § dashboard prose showed one alternative ordering
  (`12,400 ctx (11,900 cached) · …`). Spec said "e.g."; either is fine.

**Reviewed by:** reviewer agent · 2026-07-26T22:15:00Z
