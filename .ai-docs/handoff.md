# Handoff — 2026-07-19

**Branch:** `main` (clean; this handoff lands via its own PR)
**Last action:** **SessionScope (#308) fully shipped, published, documented, and closed.** Stack #309 (core `SessionScope` + wire) → #310 (typed playground scope form + presets) → #311 (scope-aware render: `renderInitialPrompt(ctx)` + `Awareness.fromScope`) merged via `st`; deploy #312 published core@0.13.0/lockstep@0.28.0; docs #313 (+ADR-0005, READMEs, `build-on-agentic-patterns` skill in BOTH homes incl. the cli plugin template) + docs-patch #314 published 0.13.1/0.28.1; #315 fixed the runtime README's fabricated `Conversation` example. Gate 2.5 joined verdict PASS_WITH_NOTES (spec + both review sections at `.ai-docs/stacks/session-scope/spec.md`). Issues #308 AND #268 closed (acceptance satisfied). Test-bench agents live in `examples/agents/`: `support-desk` (Mode B, no hook), `scope-echo` (keyless FunctionStep echo — THE e2e regression probe).
**Next action:** Pick one: (1) **consumer migrations** — canvas-workstation (half-migrated: already has instantiate seam; declare a SessionScope, kill the ORG_ID env pin) then dealbrain `dugshub/workspace-agent-v0` (kills per-message rebuild + fixed dev-seller pin); both need live-fetched prompt data pre-resolved into scope or kept on `instantiate` → they'll pressure-test whether an async `resolveScope` hook gets promoted from ADR-0005's deferral. (2) **#234** — likely root cause posted on the issue: duplicate `Transfer-Encoding: chunked` on SSE under bun. (3) **#281** menu polish sweep.
**Obstacles:**
- Port 5173 is held by the user's `~/Projects/dealbrain-sdc-fix` Vite; demos ran on **5174** (`cd packages/agent-dashboard && bun x vite --port 5174 --strictPort`, backend `... playground examples/agents --port 3456 --no-dashboard`). Demo servers from this session are background tasks and will be dead next session.
- Keyless-chat limitation unchanged (see 2026-07-16 handoff in git history); `scope-echo` sidesteps it (FunctionStep, zero tokens).

## Notes
- Small follow-ups not yet tracked: `_renderCtx` duplicated in AgentRunner + ClaudeCodeRunner; DX doc comment on `SessionScope` defaults ("defaults must cover every required field or an empty POST 400s"); scope-form nits (nested-popover clipping in scrollable panels, touched-form posts explicit `false` for untouched booleans).
- Untracked leftovers, deliberate: `.ai-docs/design/playground-menus/iterations/` (grader screenshots, regenerable) and `.ai-docs/unruliness-audit.md` (provenance unknown, appeared 2026-07-18 — has a plausible Tier-0 finding re justfile→pnpm breakage; user should skim before deleting).
- Ultracode pipeline that worked (5 workflows, ~40 agents): scout maps w/ file:line anchors → lead locks decisions → build-order-sequenced sonnet implementers → CI-equivalent gate → 3-lens Fable review (verify-before-report) → fix loop until dry. Scout phase caught the design-breaking fact (`host.deps` is a DepReader → sibling `host.scope` key).
